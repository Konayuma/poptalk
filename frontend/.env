# Browser-facing API prefix. Keep the default when using the Vite development proxy.
VITE_API_URL=/api/v1

# Laravel (or another backend) origin used only by the local Vite development server.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
